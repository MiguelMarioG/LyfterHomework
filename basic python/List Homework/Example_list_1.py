first_list = ['hay', 'en', 'que', 'iteracion', 'indices', 'muy']
second_list = ['casos', 'los', 'la', 'por', 'es', 'util']
index = 0

while (index < len (first_list)):
    record_one = first_list[index]
    record_two = second_list[index]
    print (f"{record_one} {record_two}")
    index += 1
