rol_usuario = "invitado"
if not rol_usuario == "admin":
    print("Acceso restringido. Solo administradores.")


datos_guardados = None
if not datos_guardados:
    print("No hay datos cargados, iniciando recuperación...")


activo = True
print(not activo)